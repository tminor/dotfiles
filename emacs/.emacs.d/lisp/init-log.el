;;; init-log.el --- Configure Emacs for viewing log files -*- lexical-binding: t -*-
;;
;;; Commentary:
;;
;; Install and configure modes for interacting with log files.
;;
;;; Code:

(use-package log4j-mode)
(use-package logview)
(use-package hide-lines)
(use-package syslog-mode)

(provide 'init-log)
;; init-log.el ends here
